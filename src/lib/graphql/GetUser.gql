query GetUser($id: ID!) {
	getUser(id: $id) {
		email
		id
		investingEntities {
			name
			address
			entityType
			strategy
			id
			companies {
				id
				attributes {
					booleanValue
					stringValue
				}
			}
		}
	}
}
