query GetCompany($id: ID!) {
	getCompany(id: $id) {
		id
		attributes {
			field {
				description
				id
				name
			}
			stringValue
		}

		documentCollections {
			name
			id
		}
		investingEntity {
			name
			id
			investmentCriteria {
				enabled
				required
				rules
				field {
					fieldType {
						name
					}
					name
					id
				}
			}
		}
	}
}
